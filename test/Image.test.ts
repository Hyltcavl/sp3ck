import request from "supertest";

describe("Serving pictures test suite", () => {
  it("Request /picture should return Pong!", async () => {
    expect(false).toBeFalsy();
    /* const result = await request(app).get("/image").send();

    expect(result.status).toBe(200);
    expect(result.body.data).toBe("Pong!"); */
  });
});
